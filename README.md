## Things I learned
- If you want to get an element in a `vec` with a variable index, the index needs to be `usize`
- You can pass argument to the binarie ran by `cargo run` with `-- --option`
- more to come...